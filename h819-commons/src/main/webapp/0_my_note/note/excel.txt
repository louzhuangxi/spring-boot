查看 javaeye 论坛所有的帖子

jxl问题：  竟然出现这种情况
如果读两列，当第一、第二列没有内容 ，这时如果第三列没有内容的时候，发生异常！
导入北京老库地标的例子。


本 eclipse 工程中的 BuyStdExcel 为最完善的 excel 操作实例，完善他

如果有合并的单元格，程序会自动终止，要去除

====
Cell[] cells = sheets[sheetNum].getRow(i);
方法只返回有内容的行的单元格，如果 cell 没有内容，是不会包含在 Cell[] 的，读取的时候会越界.

====