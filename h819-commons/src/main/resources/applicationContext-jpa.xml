<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xmlns="http://www.springframework.org/schema/beans"

       xsi:schemaLocation="
	   http://www.springframework.org/schema/tx
	   http://www.springframework.org/schema/tx/spring-tx-4.0.xsd
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
       http://www.springframework.org/schema/data/jpa
	   http://www.springframework.org/schema/data/jpa/spring-jpa-1.3.xsd"

       default-lazy-init="true">

    <description>Spring 模版配置，其他项目在此基础上修改，参考 Spring side4，做了修改，不要照搬</description>

    <import resource="applicationContext-dataSource.xml"></import>

    <import resource="applicationContext-email.xml"></import>

    <!-- JPA 配置开始：  Spring JPA 提供了数据库 CRUD 操作，具有事务属性，仅配置 JPA 即可-->
    <!-- Jpa entity Manager 配置 -->
    <!-- 通过配置，可自动建表 -->

    <!-- 多数据源配置未成功，暂时通过修改 dataSource 来实现吧 -->
    <!-- 需要修改 dataSource ，packagesToScan，databasePlatform ，他会扫描数据库，看该数据源是否存在表相关的表，如果不存在，报异常
     -->

    <!-- ============================== mysql  ==============================  -->

    <bean id="entityManagerFactoryMySQL" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="dataSourceMySQL"/>
        <property name="packagesToScan" value="org.h819.web.spring,org.examples.spring"/>
        <!-- 客户端调用 JPA Transaction 用到.
        <property name="jpaDialect">
            <bean class="org.springapp.app.utils.dialect.CustomHibernateJpaDialect"/>
        </property> -->
        <property name="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
                <property name="generateDdl" value="true"/>
                <property name="showSql" value="true"/>
                <property name="databasePlatform" value="org.hibernate.dialect.MySQL5Dialect"/>
            </bean>
        </property>
        <property name="jpaProperties">
            <props>
                <prop key="hibernate.ejb.naming_strategy">org.hibernate.cfg.ImprovedNamingStrategy</prop>
            </props>
        </property>
    </bean>

    <jpa:repositories base-package="org.h819.web.spring,org.examples.spring"
                      transaction-manager-ref="transactionManagerMySQL"
                      entity-manager-factory-ref="entityManagerFactoryMySQL"/>

    <bean id="transactionManagerMySQL" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactoryMySQL"/>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManagerMySQL" proxy-target-class="true"/>

</beans>